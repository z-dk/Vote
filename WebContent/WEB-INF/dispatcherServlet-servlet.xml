<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd">
	
	<!-- springmvc 配置文件 -->
	<context:component-scan base-package="vote" use-default-filters="false">
		<context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
	</context:component-scan>
	<!-- 配置视图解析器 -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/views/"></property>
		<property name="suffix" value=".jsp"></property>
	</bean>
	<!--配置拦截器，拦截非法请求  -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/admin/**"/>
			<mvc:exclude-mapping path="/admin/check"/>
			<mvc:exclude-mapping path="/admin/"/>
			<bean id="adminInterceptor" class="interceptor.AdminInterceptor"></bean>
		</mvc:interceptor>
		<mvc:interceptor>
			<mvc:mapping path="/user/**"/>
			<mvc:exclude-mapping path="/user/check"/>
			<mvc:exclude-mapping path="/user/checkuser"/>
			<mvc:exclude-mapping path="/user/adduser"/>
			<mvc:exclude-mapping path="/user/findpwd"/>
			<bean id="userInterceptor" class="interceptor.UserInterceptor"></bean>
		</mvc:interceptor>
	</mvc:interceptors>
	<!-- 将springmvc不能处理的资源交给tomcat处理 -->
	<mvc:default-servlet-handler/>
	<!-- 支持更高级的springmvc的功能 jsr303 -->
	<mvc:annotation-driven></mvc:annotation-driven>

</beans>
